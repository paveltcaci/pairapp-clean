diff --git a/pairapp_mobile/lib/features/home/home_screen.dart b/pairapp_mobile/lib/features/home/home_screen.dart
index 9ac157e..4d34543 100644
--- a/pairapp_mobile/lib/features/home/home_screen.dart
+++ b/pairapp_mobile/lib/features/home/home_screen.dart
@@ -268,6 +268,57 @@ class _HomeScreenState extends State<HomeScreen> {
     );
   }
 
+  // ── Fact block ─────────────────────────────────────────────────────────────
+
+  Widget _buildFactBlock(MilestoneFact fact) {
+    final isExact = fact.isExact;
+    return Container(
+      padding: const EdgeInsets.symmetric(horizontal: 12, vertical: 10),
+      decoration: BoxDecoration(
+        color: isExact
+            ? AppColors.purple.withValues(alpha: 0.14)
+            : AppColors.bgCardLight.withValues(alpha: 0.6),
+        borderRadius: BorderRadius.circular(10),
+        border: Border.all(
+          color: isExact
+              ? AppColors.purpleLight.withValues(alpha: 0.35)
+              : AppColors.textMuted.withValues(alpha: 0.18),
+        ),
+      ),
+      child: Column(
+        crossAxisAlignment: CrossAxisAlignment.start,
+        children: [
+          Row(
+            children: [
+              Text(
+                isExact ? '✨ ' : '💡 ',
+                style: const TextStyle(fontSize: 12),
+              ),
+              Text(
+                fact.title,
+                style: TextStyle(
+                  fontSize: 11,
+                  fontWeight: FontWeight.w600,
+                  color: isExact ? AppColors.lavender : AppColors.textSecondary,
+                  letterSpacing: 0.3,
+                ),
+              ),
+            ],
+          ),
+          const SizedBox(height: 4),
+          Text(
+            fact.body,
+            style: TextStyle(
+              fontSize: 12,
+              color: isExact ? AppColors.textPrimary : AppColors.textSecondary,
+              height: 1.45,
+            ),
+          ),
+        ],
+      ),
+    );
+  }
+
   /// CTA shown when no date has been set yet.
   Widget _buildRelationshipCta(BuildContext context) {
     return AppCard(
@@ -416,6 +467,9 @@ class _HomeScreenState extends State<HomeScreen> {
               ],
             ),
           ),
+          // ── Milestone fact ──────────────────────────────────────────────
+          const SizedBox(height: 10),
+          _buildFactBlock(breakdown.milestoneFact),
           // Confirmation status
           if (!fullyConfirmed) ...[
             const SizedBox(height: 10),
